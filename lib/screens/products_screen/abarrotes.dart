import 'package:flutter/material.dart';

class AbarrotesScreen extends StatelessWidget {
  const AbarrotesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PANTALLA DE ABARROTES'),
      ),
    );
  }
}
