import 'package:flutter/material.dart';

class LimpiezaScreen extends StatelessWidget {
  const LimpiezaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PANTALLA DE LIMPIEZA'),
      ),
    );
  }
}
