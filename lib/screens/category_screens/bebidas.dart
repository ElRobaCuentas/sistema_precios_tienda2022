import 'package:flutter/material.dart';

class BebidasScreen extends StatelessWidget {
  const BebidasScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PANTALLA DE BEBIDAS'),
      ),
    );
  }
}
