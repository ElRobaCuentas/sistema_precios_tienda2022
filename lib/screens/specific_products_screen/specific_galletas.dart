import 'package:flutter/material.dart';

class SpecificGalletasScreen extends StatelessWidget {
  const SpecificGalletasScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SpecificGalletasScreen'),
      ),
    );
  }
}
