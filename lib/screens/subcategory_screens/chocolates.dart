import 'package:flutter/material.dart';

class SpecificChocolatesScreen extends StatelessWidget {
  const SpecificChocolatesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SpecificChocolatesScreen'),
      ),
    );
  }
}
